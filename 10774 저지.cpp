#include <iostream>
#include <vector>

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int j, a;
	cin >> j >> a;
	for(int i = 1 ; i <= j ; i++){
		char size;
		cin >> size;
		
	}
}