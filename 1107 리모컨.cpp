#include <cstdio>
#include <vector>
#include <cstdlib>
#include <algorithm>

using namespace std;

int main(){
	int n, m;
	scanf("%d%d", &n, &m);
	int ans = abs()
	vector<bool> v(10, true);
	for(int i = 0 ; i < m ; i++){
		int a;
		scanf("%d", &a);
		v[a] = false;
	}

	for(int i = 0 ; i < 1000000 ; i++){
		int size;
		int temp = i;
		if(i == 0) size = 0;
		else{
			while(temp != 0){
				temp /= 10;
				size++;
			}
		}
		temp = i;
		while(temp )
	}
}