#include <cstdio>
#include <vector>

using namespace std;

int main(){
	int n, m;
	scanf("%d", &n);
	vector<int> v(n+1);
	for(int i = 1 ; i <= n ; i++){
		scanf("%d", &v[i]);
	}
	scanf("%d", &m);
	for(int i = 1 ; i <= m ; i++){
		int check;
		scanf("%d", &check);
		
	}
}