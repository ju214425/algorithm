#include <cstdio>
#include <vector>

using namespace std;

int main(){
	int n, k;
	scanf("%d%d", &n, &k);
	vector<int> v(n+1);
	for(int i = 1 ; i <= n ; i++){
		scanf("%d", &v[i]);
	}

	for(int i =  n)
}